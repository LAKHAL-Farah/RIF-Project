spring:
  application:
    name: demande-service
  config:
    import: "optional:configserver:http://${CONFIG_HOST:localhost}:8888"
  mongodb:
    uri: "mongodb://${MONGO_HOST:mongodb}:27017/demande_db"